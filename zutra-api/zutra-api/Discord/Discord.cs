using System.Threading.Tasks;

namespace zutra_api.Discord {
    public static class Discord {
        public struct Props {
            // TODO Write
        }
        public static void Start(Props props) => new Task(() => { Run(props); }).Start();

        public static void Run(Props props) {
            // TODO Write
        }
    }
}